{
  "name": "copilot-page-watch-workshop",
  "version": "1.0.0",
  "private": true,
  "description": "An autonomous webpage watcher built with the GitHub Copilot SDK",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "demo": "python3 -m http.server 8000 --bind 127.0.0.1 --directory demo",
    "browser:install": "playwright install chromium",
    "test:mcp": "node --test tests/mcp.test.mjs"
  },
  "dependencies": {
    "@github/copilot": "1.0.83",
    "@playwright/mcp": "0.0.81"
  }
}
